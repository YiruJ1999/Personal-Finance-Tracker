# 数据结构
## Book
### 📘 BookRepository
负责账本表（`Books` + 动态 `book_*` 表）的管理。

#### 基础方法
- `Task<Book> EnsureBookAsync(string? displayName)`  
  确保账本存在；如果不存在则创建并返回。
- `Task<Book?> GetBookByIdAsync(int bookId)`  
  根据账本 ID 获取账本对象。
- `Task<string> GetTableNameByIdAsync(int bookId)`  
  根据账本 ID 获取对应的物理表名。
- `Task<List<Book>> ListBooksAsync()`  
  返回所有账本列表。

#### 工具方法
- `static string QuoteIdent(string name)`  
  安全转义表名，防止 SQL 注入。
- `static string BuildPhysicalTableName(string? rawName)`  
  根据账本名构造物理表名（`book_xxx`）。

#### 表结构操作
- `Task EnsureBooksTableAsync()`  
  确保 `Books` 表存在。
- `Task EnsureBookTableSchemaAsync(string tableName)`  
  确保某账本的分录表存在（如果不存在则创建，并加索引）。
- `Task ImportExistingPhysicalBooksIfAnyAsync()`  
  将已有的物理表 `book_*` 导入 `Books` 表。
- `Task DeleteBookAsync(int bookId, bool dropPhysical)`  
  删除账本记录，并可选择是否物理删除表。


---
## Record
记录流水（分录），即每一笔发生的交易。每笔分录都写入当前账本对应表（如`book_default`）。
### RecordModel
- 账户 Account
- 支出、收入 Type
- 金额 Amount
- 支出类别 Category
- 备注 Note
- 时间 Timestamp

### RecordRepository
负责账本分录记录表（动态 `book_*`）的操作。
#### 表管理
- `Task EnsureTableAsync(int bookId)`  
  确保指定账本的记录表存在。
- `Task DropBookTableAsync(int bookId)`  
  删除账本对应的物理表。

#### 记录操作
- `Task<List<Record>> ListAsync(int bookId)`  
  返回账本中所有记录，按时间倒序排序。
- `Task<Record?> GetByIdAsync(int bookId, int id)`  
  根据 ID 获取账本中的单条记录。
- `Task SaveAsync(int bookId, Record record)`  
  保存记录：若 `Id=0` 插入新记录，否则更新。
- `Task DeleteAsync(int bookId, Record record)`  
  删除指定账本中的一条记录。
- `Task DeleteAllAsync(int bookId)`  
  删除账本中所有记录。
- `Task<List<Record>> GetRecordsPagedAsync(int bookId, int pageNumber, int pageSize)`  
  分页获取账本记录。

#### 数据迁移工具
- `private Task BackfillAccountIdsAsync(string table)`  
  补齐旧数据中缺失的 `AccountId` 字段。
---

## Account
派生数据，从Record中聚合出来。通过扫描一个或多个`book_*`表，聚合出每个账户的余额、列表；可支持手动新增账户。

### AccountModel
- String Name
- decimal Balance
- string Type
- DateTime CreatedAt

### AccountRepository
负责账户表（`Account`）的管理和聚合。

#### 基础表操作
- `Task EnsureDatabaseInitializedAsync()`  
  确保 `Account` 表存在。
- `Task<List<Account>> ListAsync()`  
  返回 `Account` 表中所有账户。
- `Task<Account> AddAccountAsync(string name)`  
  新增账户（余额为 0，若已存在则返回已有账户）。
- `Task<Account> AddAccountAsync(string name, decimal openingBalance)`  
  新增账户（设置初始余额），若已存在则更新余额。
- `Task DeleteAccountAsync(int accountId, bool alsoDeleteRecords)`  
  删除账户，若指定则同时删除账本中对应的记录。
- `Task UpdateAccountBalanceAsync(int accountId, decimal newBalance)`  
  更新账户余额。

#### 多账本聚合相关
- `Task<decimal> GetTotalAssetsAsync()`  
  计算所有账户余额总和。
- `Task<Dictionary<string, decimal>> GetLast4MonthsTotalAssetsAsync()`  
  计算最近 4 个月月底的总资产趋势。
- `Task<List<(int AccountId, decimal Balance)>> GetAccountsWithBalancesFromBooksAsync(int? bookId)`  
  扫描账本表，按账户聚合余额。
- `Task SyncAccountsFromBooksAsync(int? bookId)`  
  将账本中的余额同步到账户表。

#### ### 跨账本查询
- `Task<List<Record>> ListRecordsForAccountAcrossBooksAsync(int accountId, DateTime start, DateTime end)`  
  查询某账户在所有账本中的流水记录。

#### 兼容接口（供 PageModel 调用）
- **SyncAccountsFromRecordsAsync()**
  - 向后兼容旧方法名，实际调用 `SyncAccountsFromBooksAsync()` 扫描所有账本。

- **GetAccountsWithBalancesAsync()**
  - 返回 `Account` 表中所有账户及其余额（调用前应先同步）。

- **GetTotalAssetsAsync()**
  - 计算并返回所有账户余额的总和。

- **GetLast4MonthsTotalAssetsAsync()**
  - 计算最近 4 个月末的总资产趋势：
    - 从所有账本读取记录；
    - 统计每个月底的累计收支（收入正、支出负）；
    - 返回字典 `{ "YYYY-MM": 总额 }`。

### Record和Account之间数据流
1. **新增记录**  
   `RecordRepository.SaveAsync(bookId, record)` → 插入 `book_xxx`
2. **同步账户**  
   `AccountRepository.SyncAccountsFromBooksAsync(bookId)` → 更新 `Account` 表余额
3. **页面展示**  
   `AccountPageModel.LoadAsync()` → 同步并读取账户与资产
4. **多账本模式**  
   - 流水存储在 `book_xxx` 表  
   - `Account` 表作为缓存，需要定期同步
   - 
---
## PersonalInfo
### 数据模型：PersonalInfoModel
- **id** (`int`)
  - 主键，自增（`[PrimaryKey, AutoIncrement]`）。
- **Name** (`string`)
  - 用户名称（默认随机生成 5 个字符）。
- **AvatarPath** (`string`)
  - 用户头像路径（默认 `"default_avatar.png"`）。
- **表名**：`PersonalInfos`


### 仓储类：PersonalInfoRepository

#### 基础
- **EnsureDatabaseInitializedAsync()**
  - 调用 `DatabaseService.InitAsync()` 初始化数据库连接与表结构。

#### 查询与初始化
- **GetPersonalInfoAsync()**
  - 从 `PersonalInfos` 表读取第一条个人信息；
  - 如果表为空：
    - 随机生成一个 5 位字符串作为 `Name`；
    - 设置 `AvatarPath = "default_avatar.png"`；
    - 插入并返回该默认信息。

#### 保存
- **SaveAsync(PersonalInfo personalInfo)**
  - 如果 `personalInfo.id == 0`：插入新记录；
  - 否则：更新现有记录。

#### 列表
- **ListAsync()**
  - 返回 `PersonalInfos` 表中的所有记录。

#### 工具方法（私有）
- **GenerateRandomName()**
  - 生成一个包含大小写字母与数字的 5 位随机字符串。

### 数据流关系
- **首次使用时**：`GetPersonalInfoAsync()` 会自动生成一个默认用户（随机名 + 默认头像）。
- **修改个人信息**：通过 `SaveAsync()` 更新记录。
- **读取全部用户**：调用 `ListAsync()`。