# 数据结构
## Book
---
## Record
记录流水（分录），即每一笔发生的交易。每笔分录都写入当前账本对应表（如`book_default`）。
### RecordModel
- 账户 Account
- 支出、收入 Type
- 金额 Amount
- 支出类别 Category
- 备注 Note
- 时间 Timestamp

### RecordRepository
#### 私有工具方法
- **GetRecordTableName(string bookName)**
  - 将账本名 `bookName` 转为安全的表名（只保留字母、数字和下划线）；
  - 最终格式为 `book_{SanitizedName}`；
  - 用于防止 SQL 注入及保证表名合法。

- **EnsureTableAsync(string bookName)**
  - 确保对应账本的记录表存在（如果不存在则创建）；
  - 表字段包括：`ID`（主键自增）、`Type`、`Amount`、`Category`、`Note`、`Timestamp`。

#### 公共方法
- **ListAsync(string bookName)**
  - 返回指定账本表中的所有记录，按 `Timestamp` 倒序排序。

- **GetByIdAsync(string bookName, int id)**
  - 根据记录 ID 获取指定账本中的单条记录；
  - 如果不存在，返回 `null`。

- **SaveAsync(string bookName, Record record)**
  - 保存一条记录到指定账本：
    - 如果 `record.ID == 0`：插入新记录，并将生成的 ID 回写到 `record.ID`；
    - 否则：更新已有记录。

- **DeleteAsync(string bookName, Record record)**
  - 删除指定账本中与 `record.ID` 匹配的记录。

- **DeleteAllAsync(string bookName)**
  - 删除指定账本表中的所有记录。

- **GetRecordsPagedAsync(string bookName, int pageNumber, int pageSize)**
  - 分页获取指定账本的记录：
    - 按 `Timestamp` 倒序；
    - `pageNumber` 为页码（从 1 开始），`pageSize` 为每页条数。
  
---

## Account
派生数据，从Record中聚合出来。通过扫描一个或多个`book_*`表，聚合出每个账户的余额、列表；可支持手动新增账户。

### AccountModel
- String Name
- decimal Balance
- string Type
- DateTime CreatedAt

### AccountRepository
#### 基础表操作
- **EnsureDatabaseInitializedAsync()**
  - 确保 `Account` 表存在（不存在则创建）。

- **ListAsync()**
  - 返回 `Account` 表中的所有账户记录。

- **AddAccountAsync(string name)**
  - 如果账户不存在，则添加一个名称为 `name`、余额为 `0` 的新账户。

- **AddAccountAsync(string name, decimal openingBalance)**
  - 如果账户不存在，则添加一个名称为 `name`、余额为 `openingBalance` 的新账户；
  - 如果已存在，则直接更新余额为 `openingBalance`。

- **DeleteAccountAsync(string name, bool alsoDeleteRecords)**
  - 删除名称为 `name` 的账户；
  - 如果 `alsoDeleteRecords` 为 `true`，则同时删除所有账本（`book_*` 表）中该账户的记录。

- **UpdateAccountBalanceAsync(string name, decimal newBalance)**
  - 直接将账户表中 `name` 对应账户的余额设置为 `newBalance`。


#### 多账本聚合相关
- **GetBookTableNamesAsync()**
  - 获取数据库中所有账本表（`book_*`）的表名列表。

- **GetAccountsWithBalancesFromBooksAsync(string? book = null)**
  - 扫描一个或全部账本表，按账户聚合余额：
    - `收入` 类型的记录 → 金额正向累加；
    - `支出` 类型的记录 → 金额负向累加；
    - 其它类型默认不计。

- **SyncAccountsFromBooksAsync(string? book = null)**
  - 基于 `GetAccountsWithBalancesFromBooksAsync()` 的聚合结果，更新或插入 `Account` 表中的账户余额。

#### 兼容接口（供 PageModel 调用）
- **SyncAccountsFromRecordsAsync()**
  - 向后兼容旧方法名，实际调用 `SyncAccountsFromBooksAsync()` 扫描所有账本。

- **GetAccountsWithBalancesAsync()**
  - 返回 `Account` 表中所有账户及其余额（调用前应先同步）。

- **GetTotalAssetsAsync()**
  - 计算并返回所有账户余额的总和。

- **GetLast4MonthsTotalAssetsAsync()**
  - 计算最近 4 个月末的总资产趋势：
    - 从所有账本读取记录；
    - 统计每个月底的累计收支（收入正、支出负）；
    - 返回字典 `{ "YYYY-MM": 总额 }`。

### Record和Account之间数据流
1. 新增记录
   - AddRecordPageModel.Save() 调用 RecordRepository.SaveAsync(CurrentBook, record)
   - 记录被插入对应账本表（如 book_default）。

2. 同步账户
   - 保存记录后，AddRecordPageModel 会调用 AccountRepository.SyncAccountsFromBooksAsync(CurrentBook)
   - 账户仓储扫描指定账本表，按账户聚合余额，并更新 Account 表。

3. 账户页面加载
   - AccountPageModel.LoadAsync() 会调用：`SyncAccountsFromRecordsAsync()` → 实际是 `SyncAccountsFromBooksAsync(null)`，扫描所有账本表；
   - GetAccountsWithBalancesAsync() → 从 Account 表读出账户及余额；
   - GetTotalAssetsAsync() → 汇总 Account 表余额；
   - GetLast4MonthsTotalAssetsAsync() → 扫描所有账本表记录，按月份累计收支生成趋势图。

4. 多账本模式的核心
   - 所有流水数据在 动态账本表 中（book_xxx）；
   - Account 表只是缓存/维度表，需要定期通过扫描账本表来同步。
   - 
---
## PersonalInfo
### 数据模型：PersonalInfoModel
- **id** (`int`)
  - 主键，自增（`[PrimaryKey, AutoIncrement]`）。
- **Name** (`string`)
  - 用户名称（默认随机生成 5 个字符）。
- **AvatarPath** (`string`)
  - 用户头像路径（默认 `"default_avatar.png"`）。
- **表名**：`PersonalInfos`


### 仓储类：PersonalInfoRepository

#### 基础
- **EnsureDatabaseInitializedAsync()**
  - 调用 `DatabaseService.InitAsync()` 初始化数据库连接与表结构。

#### 查询与初始化
- **GetPersonalInfoAsync()**
  - 从 `PersonalInfos` 表读取第一条个人信息；
  - 如果表为空：
    - 随机生成一个 5 位字符串作为 `Name`；
    - 设置 `AvatarPath = "default_avatar.png"`；
    - 插入并返回该默认信息。

#### 保存
- **SaveAsync(PersonalInfo personalInfo)**
  - 如果 `personalInfo.id == 0`：插入新记录；
  - 否则：更新现有记录。

#### 列表
- **ListAsync()**
  - 返回 `PersonalInfos` 表中的所有记录。

#### 工具方法（私有）
- **GenerateRandomName()**
  - 生成一个包含大小写字母与数字的 5 位随机字符串。

### 数据流关系
- **首次使用时**：`GetPersonalInfoAsync()` 会自动生成一个默认用户（随机名 + 默认头像）。
- **修改个人信息**：通过 `SaveAsync()` 更新记录。
- **读取全部用户**：调用 `ListAsync()`。