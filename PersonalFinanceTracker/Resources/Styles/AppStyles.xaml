<?xml version="1.0" encoding="UTF-8" ?>
<!--
  Personal Finance Tracker
  File: PersonalFinanceTracker/Resources/Styles/AppStyles.xaml
  Purpose: Defines shared visual resources for the MAUI application.
-->
<?xaml-comp compile="true" ?>
<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:f="clr-namespace:Fonts"
    xmlns:pullToRefresh="clr-namespace:Syncfusion.Maui.Toolkit.PullToRefresh;assembly=Syncfusion.Maui.Toolkit"
    xmlns:sf="clr-namespace:Syncfusion.Maui.Toolkit.TextInputLayout;assembly=Syncfusion.Maui.Toolkit"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:behaviors="clr-namespace:PersonalFinanceTracker.Behaviors">

    <!-- ========= Spacing tokens (kept) ========= -->
    <x:Double x:Key="sizeNone">0</x:Double>
    <x:Double x:Key="size20">2</x:Double>
    <x:Double x:Key="size40">4</x:Double>
    <x:Double x:Key="size60">6</x:Double>
    <x:Double x:Key="size80">8</x:Double>
    <x:Double x:Key="size100">10</x:Double>
    <x:Double x:Key="size120">12</x:Double>
    <x:Double x:Key="size160">16</x:Double>
    <x:Double x:Key="size200">20</x:Double>
    <x:Double x:Key="size240">24</x:Double>
    <x:Double x:Key="size280">28</x:Double>
    <x:Double x:Key="size320">32</x:Double>
    <x:Double x:Key="size360">36</x:Double>
    <x:Double x:Key="size400">40</x:Double>
    <x:Double x:Key="size480">48</x:Double>
    <x:Double x:Key="size520">52</x:Double>
    <x:Double x:Key="size560">56</x:Double>

    <!-- ========= Icon sizes (kept; slightly tuned via OnIdiom) ========= -->
    <OnIdiom x:Key="IconSize" x:TypeArguments="x:Double" Default="20">
        <OnIdiom.Desktop>32</OnIdiom.Desktop>
    </OnIdiom>
    <OnIdiom x:Key="IconSizeSmall" x:TypeArguments="x:Double" Default="12">
        <OnIdiom.Desktop>18</OnIdiom.Desktop>
    </OnIdiom>

    <!-- Layout paddings/spacings (kept) -->
    <OnIdiom x:Key="LayoutPadding" x:TypeArguments="Thickness" Default="15">
        <OnIdiom.Desktop>30</OnIdiom.Desktop>
    </OnIdiom>
    <OnIdiom x:Key="LayoutSpacing" x:TypeArguments="x:Double" Default="5">
        <OnIdiom.Desktop>15</OnIdiom.Desktop>
    </OnIdiom>

    <!-- ========= Icons (kept) ========= -->
    <FontImageSource x:Key="IconDashboard"
                     Glyph="{x:Static f:FluentUI.diagram_24_regular}"
                     FontFamily="{x:Static f:FluentUI.FontFamily}"
                     Color="{AppThemeBinding Light={StaticResource DarkOnLightBackground}, Dark={StaticResource LightOnDarkBackground}}"
                     Size="{StaticResource IconSize}" />
    <FontImageSource x:Key="IconProjects"
                     Glyph="{x:Static f:FluentUI.list_24_regular}"
                     FontFamily="{x:Static f:FluentUI.FontFamily}"
                     Color="{AppThemeBinding Light={StaticResource DarkOnLightBackground}, Dark={StaticResource LightOnDarkBackground}}"
                     Size="{StaticResource IconSize}" />
    <FontImageSource x:Key="IconMeta"
                     Glyph="{x:Static f:FluentUI.info_24_regular}"
                     FontFamily="{x:Static f:FluentUI.FontFamily}"
                     Color="{AppThemeBinding Light={StaticResource DarkOnLightBackground}, Dark={StaticResource LightOnDarkBackground}}"
                     Size="{StaticResource IconSize}" />
    <FontImageSource x:Key="IconRibbon"
                     Glyph="{x:Static f:FluentUI.ribbon_20_regular}"
                     FontFamily="{x:Static f:FluentUI.FontFamily}"
                     Color="{AppThemeBinding Light={StaticResource DarkOnLightBackground}, Dark={StaticResource LightOnDarkBackground}}"
                     Size="{StaticResource IconSize}" />
    <FontImageSource x:Key="IconAdd"
                     Glyph="{x:Static f:FluentUI.add_32_regular}"
                     FontFamily="{x:Static f:FluentUI.FontFamily}"
                     Color="{AppThemeBinding Light={StaticResource White}, Dark={StaticResource LightOnDarkBackground}}"
                     Size="{StaticResource IconSize}" />
    <FontImageSource x:Key="IconDelete"
                     Glyph="{x:Static f:FluentUI.delete_32_regular}"
                     FontFamily="{x:Static f:FluentUI.FontFamily}"
                     Color="{AppThemeBinding Light={StaticResource DarkOnLightBackground}, Dark={StaticResource LightOnDarkBackground}}"
                     Size="{StaticResource IconSize}" />
    <FontImageSource x:Key="IconClean"
                     Glyph="{x:Static f:FluentUI.broom_32_regular}"
                     FontFamily="{x:Static f:FluentUI.FontFamily}"
                     Color="{AppThemeBinding Light={StaticResource DarkOnLightBackground}, Dark={StaticResource LightOnDarkBackground}}"
                     Size="{StaticResource size240}" />
    <FontImageSource x:Key="IconLight"
                     Glyph="{x:Static f:FluentUI.weather_sunny_28_regular}"
                     FontFamily="{x:Static f:FluentUI.FontFamily}"
                     Color="{AppThemeBinding Light={StaticResource DarkOnLightBackground}, Dark={StaticResource LightOnDarkBackground}}"
                     Size="{StaticResource size240}" />
    <FontImageSource x:Key="IconDark"
                     Glyph="{x:Static f:FluentUI.weather_moon_28_regular}"
                     FontFamily="{x:Static f:FluentUI.FontFamily}"
                     Color="{AppThemeBinding Light={StaticResource DarkOnLightBackground}, Dark={StaticResource LightOnDarkBackground}}"
                     Size="{StaticResource size240}" />

    <!-- ========= Adaptive base for all Labels =========
         This base injects responsive font behavior. Other label styles will BasedOn this. -->
    <Style x:Key="AdaptiveLabelBase" TargetType="Label">
        <!-- Keep your colors -->
        <Setter Property="TextColor"
                Value="{AppThemeBinding Light={StaticResource DarkOnLightBackground}, Dark={StaticResource LightOnDarkBackground}}" />
        <Setter Property="BackgroundColor" Value="Transparent" />
        <!-- Fallback size (used before layout measurement) -->
        <Setter Property="FontSize" Value="17" />
        <Setter Property="LineHeight" Value="1.29" />
        <!-- Visual states (kept) -->
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="TextColor"
                                    Value="{AppThemeBinding Light={StaticResource Gray300}, Dark={StaticResource Gray600}}" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>

        <!-- ✅ Hook responsive font via attached properties.
             Ratio ~ 0.045 works well for body text on phones; Dimension=Min stabilizes on rotation. -->
        <Setter Property="behaviors:ResponsiveFont.Enable" Value="True"/>
        <Setter Property="behaviors:ResponsiveFont.Dimension" Value="Min"/>
        <Setter Property="behaviors:ResponsiveFont.Ratio" Value="0.045"/>
        <Setter Property="behaviors:ResponsiveFont.MinFontSize" Value="12"/>
        <Setter Property="behaviors:ResponsiveFont.MaxFontSize" Value="22"/>
    </Style>

    <!-- ========= Concrete label styles (now BasedOn the adaptive base) ========= -->
    <Style TargetType="Label" BasedOn="{StaticResource AdaptiveLabelBase}" />

    <Style x:Key="Caption2" TargetType="Label" BasedOn="{StaticResource AdaptiveLabelBase}">
        <!-- Smaller copy -->
        <Setter Property="behaviors:ResponsiveFont.Ratio" Value="0.035"/>
        <Setter Property="behaviors:ResponsiveFont.MinFontSize" Value="11"/>
        <Setter Property="behaviors:ResponsiveFont.MaxFontSize" Value="16"/>
        <Setter Property="LineHeight" Value="1.33"/>
    </Style>

    <Style x:Key="Caption1" TargetType="Label" BasedOn="{StaticResource AdaptiveLabelBase}">
        <Setter Property="behaviors:ResponsiveFont.Ratio" Value="0.038"/>
        <Setter Property="behaviors:ResponsiveFont.MinFontSize" Value="12"/>
        <Setter Property="behaviors:ResponsiveFont.MaxFontSize" Value="18"/>
        <Setter Property="LineHeight" Value="1.38"/>
    </Style>

    <Style x:Key="Caption1Strong" TargetType="Label" BasedOn="{StaticResource Caption1}">
        <Setter Property="FontFamily" Value="{OnPlatform WinUI=SegoeSemibold, iOS=.SFUI-SemiBold, MacCatalyst=.SFUI-SemiBold, Default=''}" />
        <Setter Property="FontAttributes" Value="{OnPlatform Default=None, Android=Bold}" />
    </Style>

    <Style x:Key="Body2" TargetType="Label" BasedOn="{StaticResource AdaptiveLabelBase}">
        <Setter Property="behaviors:ResponsiveFont.Ratio" Value="0.043"/>
        <Setter Property="behaviors:ResponsiveFont.MinFontSize" Value="13"/>
        <Setter Property="behaviors:ResponsiveFont.MaxFontSize" Value="20"/>
        <Setter Property="LineHeight" Value="1.33"/>
    </Style>

    <Style x:Key="Body2Strong" TargetType="Label" BasedOn="{StaticResource Body2}">
        <Setter Property="FontFamily" Value="{OnPlatform WinUI=SegoeSemibold, iOS=.SFUI-SemiBold, MacCatalyst=.SFUI-SemiBold, Default=''}" />
        <Setter Property="FontAttributes" Value="{OnPlatform Default=None, Android=Bold}" />
    </Style>

    <Style x:Key="Body1" TargetType="Label" BasedOn="{StaticResource AdaptiveLabelBase}">
        <!-- Default body -->
        <Setter Property="behaviors:ResponsiveFont.Ratio" Value="0.045"/>
        <Setter Property="behaviors:ResponsiveFont.MinFontSize" Value="12"/>
        <Setter Property="behaviors:ResponsiveFont.MaxFontSize" Value="22"/>
        <Setter Property="LineHeight" Value="1.29"/>
    </Style>

    <Style x:Key="Body1Strong" TargetType="Label" BasedOn="{StaticResource Body1}">
        <Setter Property="FontFamily" Value="{OnPlatform WinUI=SegoeSemibold, iOS=.SFUI-SemiBold, MacCatalyst=.SFUI-SemiBold, Default=''}" />
        <Setter Property="FontAttributes" Value="{OnPlatform Default=None, Android=Bold}" />
    </Style>

    <Style x:Key="Title3" TargetType="Label" BasedOn="{StaticResource AdaptiveLabelBase}">
        <Setter Property="behaviors:ResponsiveFont.Ratio" Value="0.055"/>
        <Setter Property="behaviors:ResponsiveFont.MinFontSize" Value="16"/>
        <Setter Property="behaviors:ResponsiveFont.MaxFontSize" Value="26"/>
        <Setter Property="LineHeight" Value="1.25"/>
        <Setter Property="FontFamily" Value="{OnPlatform WinUI=SegoeSemibold, iOS=.SFUI-SemiBold, MacCatalyst=.SFUI-SemiBold, Default=''}" />
        <Setter Property="FontAttributes" Value="{OnPlatform Default=None, Android=Bold}" />
    </Style>

    <Style x:Key="Title2" TargetType="Label" BasedOn="{StaticResource AdaptiveLabelBase}">
        <Setter Property="behaviors:ResponsiveFont.Ratio" Value="0.060"/>
        <Setter Property="behaviors:ResponsiveFont.MinFontSize" Value="18"/>
        <Setter Property="behaviors:ResponsiveFont.MaxFontSize" Value="28"/>
        <Setter Property="LineHeight" Value="1.27"/>
        <Setter Property="FontFamily" Value="{OnPlatform WinUI=SegoeSemibold, iOS=.SFUI-SemiBold, MacCatalyst=.SFUI-SemiBold, Default=''}" />
        <Setter Property="FontAttributes" Value="{OnPlatform Android=Bold, Default=None}" />
    </Style>

    <Style x:Key="Title1" TargetType="Label" BasedOn="{StaticResource AdaptiveLabelBase}">
        <Setter Property="behaviors:ResponsiveFont.Ratio" Value="0.070"/>
        <Setter Property="behaviors:ResponsiveFont.MinFontSize" Value="22"/>
        <Setter Property="behaviors:ResponsiveFont.MaxFontSize" Value="34"/>
        <Setter Property="LineHeight" Value="1.21"/>
        <Setter Property="FontAttributes" Value="{OnPlatform WinUI=None, Default=Bold}" />
    </Style>

    <Style x:Key="LargeTitle" TargetType="Label" BasedOn="{StaticResource AdaptiveLabelBase}">
        <Setter Property="behaviors:ResponsiveFont.Ratio" Value="0.085"/>
        <Setter Property="behaviors:ResponsiveFont.MinFontSize" Value="26"/>
        <Setter Property="behaviors:ResponsiveFont.MaxFontSize" Value="41"/>
        <Setter Property="LineHeight" Value="1.21"/>
        <Setter Property="FontAttributes" Value="{OnPlatform WinUI=None, Default=Bold}" />
    </Style>

    <Style x:Key="Display" TargetType="Label" BasedOn="{StaticResource AdaptiveLabelBase}">
        <Setter Property="behaviors:ResponsiveFont.Ratio" Value="0.120"/>
        <Setter Property="behaviors:ResponsiveFont.MinFontSize" Value="34"/>
        <Setter Property="behaviors:ResponsiveFont.MaxFontSize" Value="70"/>
        <Setter Property="LineHeight" Value="1.17"/>
        <Setter Property="FontAttributes" Value="{OnPlatform WinUI=None, Default=Bold}" />
    </Style>

    <!-- ========= Border styles (kept) ========= -->
    <Style TargetType="Border">
        <Setter Property="StrokeShape" Value="RoundRectangle 20"/>
        <Setter Property="Background" Value="{AppThemeBinding Light={StaticResource LightSecondaryBackground},Dark={StaticResource DarkSecondaryBackground}}"/>
        <Setter Property="StrokeThickness" Value="0"/>
        <Setter Property="Padding" Value="{OnIdiom 15, Desktop=20}"/>
    </Style>

    <Style TargetType="Border" x:Key="CardStyle">
        <Setter Property="StrokeShape" Value="RoundRectangle 20"/>
        <Setter Property="Background" Value="{AppThemeBinding Light={StaticResource LightSecondaryBackground},Dark={StaticResource DarkSecondaryBackground}}"/>
        <Setter Property="StrokeThickness" Value="0"/>
        <Setter Property="Padding" Value="{OnIdiom 15, Desktop=20}"/>
    </Style>

    <!-- ========= Syncfusion TextInputLayout (kept) ========= -->
    <sf:LabelStyle x:Key="lightThemeLabelStyle" TextColor="{StaticResource DarkOnLightBackground}" FontSize="{OnIdiom 18, Desktop=24}"/>
    <sf:LabelStyle x:Key="darkThemeLabelStyle" TextColor="{StaticResource LightOnDarkBackground}" FontSize="{OnIdiom 18, Desktop=24}"/>
    <Style TargetType="sf:SfTextInputLayout">
        <Setter Property="ContainerType" Value="Outlined"/>
        <Setter Property="ContainerBackground" Value="Transparent"/>
        <Setter Property="Stroke" Value="{AppThemeBinding Light={StaticResource DarkOnLightBackground}, Dark={StaticResource LightOnDarkBackground}}"/>
        <Setter Property="HintLabelStyle" Value="{AppThemeBinding Light={StaticResource lightThemeLabelStyle}, Dark={StaticResource darkThemeLabelStyle}}"/>
    </Style>

    <!-- ========= PullToRefresh & Shimmer (kept) ========= -->
    <Style TargetType="BoxView" x:Key="ShimmerCustomViewStyle">
        <Setter Property="BackgroundColor" Value="Gray" />
        <Setter Property="HorizontalOptions" Value="FillAndExpand" />
        <Setter Property="VerticalOptions" Value="CenterAndExpand" />
    </Style>
    <Style TargetType="pullToRefresh:SfPullToRefresh">
        <Setter Property="TransitionMode" Value="SlideOnTop"/>
        <Setter Property="PullingThreshold" Value="100"/>
        <Setter Property="ProgressColor" Value="{StaticResource Primary}"/>
    </Style>

    <!-- ========= Bottom bar tokens ========= -->
    <OnIdiom x:Key="BottomBarHeight" x:TypeArguments="x:Double" Default="64">
        <!-- Slightly higher on desktop for larger hit targets -->
        <OnIdiom.Desktop>72</OnIdiom.Desktop>
    </OnIdiom>
    <Thickness x:Key="BottomBarPadding">20,0</Thickness>
    <Thickness x:Key="BottomBarButtonMargin">3,0</Thickness>
    <x:Double x:Key="BottomBarButtonCorner">10</x:Double>

    <!-- Bottom bar container -->
    <Style x:Key="BottomBarContainerStyle" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Stroke" Value="{StaticResource PrimaryDarkBrush}"/>
        <Setter Property="StrokeThickness" Value="1"/>
        <Setter Property="Padding" Value="{StaticResource BottomBarPadding}"/>
        <Setter Property="StrokeShape" Value="Rectangle"/>
        <Setter Property="HeightRequest" Value="{StaticResource BottomBarHeight}"/>
    </Style>

    <!-- Bottom bar layout -->
    <Style x:Key="BottomBarFlexStyle" TargetType="FlexLayout">
        <Setter Property="Direction" Value="Row"/>
        <Setter Property="JustifyContent" Value="SpaceBetween"/>
        <Setter Property="AlignItems" Value="Center"/>
        <Setter Property="Wrap" Value="NoWrap"/>
    </Style>

    <!-- ========= Bottom bar button (now adaptive) ========= -->
    <Style x:Key="BottomBarButtonStyle" TargetType="Button">
        <!-- Visual base -->
        <Setter Property="CornerRadius" Value="{StaticResource BottomBarButtonCorner}"/>
        <Setter Property="Padding" Value="0"/>
        <Setter Property="Margin" Value="{StaticResource BottomBarButtonMargin}"/>
        <!-- Fallback size before layout -->
        <Setter Property="FontSize" Value="{OnIdiom 14, Desktop=16}"/>
        <Setter Property="FontAutoScalingEnabled" Value="True"/>
        <Setter Property="MinimumHeightRequest" Value="{OnIdiom 44, Desktop=48}"/>

        <!-- Teal theme -->
        <Setter Property="BackgroundColor" Value="{StaticResource Primary}"/>
        <Setter Property="TextColor" Value="{StaticResource White}"/>

        <!-- States -->
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal"/>
                    <VisualState x:Name="PointerOver">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{StaticResource Primary}"/>
                            <Setter Property="Opacity" Value="0.95"/>
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Pressed">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{StaticResource PrimaryDark}"/>
                        </VisualState.Setters>
                    </VisualState>
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="{StaticResource Gray200}"/>
                            <Setter Property="TextColor" Value="{StaticResource Gray500}"/>
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>

        <!-- ✅ Adaptive font for buttons (a bit larger than body text) -->
        <Setter Property="behaviors:ResponsiveFont.Enable" Value="True"/>
        <Setter Property="behaviors:ResponsiveFont.Dimension" Value="Min"/>
        <Setter Property="behaviors:ResponsiveFont.Ratio" Value="0.050"/>
        <Setter Property="behaviors:ResponsiveFont.MinFontSize" Value="13"/>
        <Setter Property="behaviors:ResponsiveFont.MaxFontSize" Value="24"/>
    </Style>

    <!-- Primary '+' button -->
    <Style x:Key="BottomBarPrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource BottomBarButtonStyle}">
        <Setter Property="BackgroundColor" Value="{StaticResource PrimaryDark}"/>
        <Setter Property="FontAttributes" Value="Bold"/>
    </Style>

    <!-- Global ImageButton style: auto-size icons to nearby text-->
    <Style TargetType="ImageButton">
        <Setter Property="behaviors:IconAutoSize.Enable" Value="True"/>
        <Setter Property="behaviors:IconAutoSize.Scale" Value="1.1"/>
        <Setter Property="behaviors:IconAutoSize.Min" Value="12"/>
        <Setter Property="behaviors:IconAutoSize.Max" Value="28"/>
    </Style>

</ResourceDictionary>


